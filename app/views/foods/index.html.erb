  <h1>Search results</h1>
  <h3>Total results: <%= @data[:total_hits] %></h3>
  <table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">GTIN/UPC code</th>
      <th scope="col">Description</th>
      <th scope="col">Brand Owner</th>
      <th scope="col">Ingredients</th>
    </tr>
  </thead>
  <% if @data[:foods] %>
    <% @data[:foods].each_with_index do |food, index|%>
    <div class="food-<%= food.gintUpc %>">
    <tbody>
    <tr>
      <th scope="row"><%= index+1 %></th>
      <td> <%= food.gintUpc %></td>
      <td> <%= food.description %></td>
      <td> <%= food.brandOwner%></td>
      <td> <%= food.ingredients%></td>
    </div>
    </tbody>
    <% end %>
  <% end %>
  </table>
</section> 